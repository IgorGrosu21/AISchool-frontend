export type HasSchoolSlug = {
  schoolId: string
  schoolSlug: string
}

export type HasKlassSlug = {
  klassId: string
  klassSlug: string
}

export type HasGroupSlug = {
  groupId: string
  groupSlug: string
}